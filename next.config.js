/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['via.placeholder.com'],
  },
  // Konfigurasi tambahan lainnya jika diperlukan
};

module.exports = nextConfig;